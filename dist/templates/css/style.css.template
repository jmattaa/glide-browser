/* syntax: 
    #{VARIABLE_NAME}
    and normal css
*/

:root {
    --glide-bg: #{settings.theme.bg};
    --glide-fg: #{settings.theme.fg};
}

* {
    background-color: rgb(var(--glide-bg));
    color: rgb(var(--glide-fg));
}
